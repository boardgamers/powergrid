These viewer changes are for local review. Nothing has been published.

`viewer-preferences.json` is a merge patch for the eventual BGS release: enable the embedded chat and add/update the sound preference while preserving all other preferences and viewer fields. Do not PUT this partial file as a full game-info document.

The local viewer has a Playtest tools panel with individual sound buttons and a mute toggle. Chat messages entered in the local preview stay local.
